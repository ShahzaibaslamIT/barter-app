{
  "name": "@barter/db",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Shared Prisma schema and client singleton for the barter-app monorepo.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate",
    "migrate": "prisma migrate dev",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.16.1"
  },
  "devDependencies": {
    "prisma": "^6.16.1"
  }
}
